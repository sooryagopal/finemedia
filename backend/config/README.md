# Config Directory

This directory is used for securely storing configuration files that contain sensitive keys or credentials that shouldn't be hardcoded into the javascript repository.

**Instructions:**
Drop your `google-calendar-service-account.json` file into this specific folder to activate the Google Calendar synchronization!
